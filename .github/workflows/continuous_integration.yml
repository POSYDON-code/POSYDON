name: POSYDON Continuous Integration

on:
  pull_request:
    branches: [main, development]

jobs:
  test:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest]
        architecture: [x64, arm64]  # Test both architectures
        python-version: ['3.11']
      exclude:
        - os: ubuntu-latest
          architecture: arm64  # Exclude arm64 for Ubuntu as it’s unnecessary

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install POSYDON without extras
      run: |
        python -m pip install --upgrade pip
        pip install . 

    # - name: Run all tests
    #   run: |
    #     pytest posydon/unit_tests/
